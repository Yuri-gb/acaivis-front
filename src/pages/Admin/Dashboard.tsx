import {useEffect,useState} from 'react';import {api} from '../../services/api';import './Admin.css'
export default function Dashboard(){const [s,setS]=useState<any>();useEffect(()=>{api.reportSummary().then(setS)},[]);return <><div className="admin-top"><h1>Dashboard</h1></div><div className="admin-grid">{[['Pedidos',s?.totalOrders??'—'],['Entregues',s?.deliveredOrders??'—'],['Cancelados',s?.cancelledOrders??'—'],['Produtos disponíveis',s?.availableProducts??'—']].map(([a,b])=><div className="admin-card" key={String(a)}><span>{a}</span><strong>{b}</strong></div>)}</div><div className="admin-panel"><h2>Visão geral</h2><p>Use o menu para administrar o catálogo, acompanhar pedidos e consultar relatórios.</p></div></>}
